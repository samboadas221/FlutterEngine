name: sboadas
description: Simple Flutter base
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0
  audiopool: ^2.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  
  hive: ^2.2.3  # Core Hive
  hive_flutter: ^1.1.0  # Para init en Flutter
  build_runner: ^2.4.7  # Para generar adapters (dev_dependencies)
  hive_generator: ^2.0.1
  http: ^1.2.2  # Para fetch APIs
  share_plus: ^10.0.0  # Para exportar archivos
  path_provider: ^2.1.4  # Para paths de archivos

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
